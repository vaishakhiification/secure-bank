import { CredentialsDirective } from './credentials.directive';

describe('CredentailsDirective', () => {
  it('should create an instance', () => {
    const directive = new CredentialsDirective();
    expect(directive).toBeTruthy();
  });
});
